# Operator

## Claude Code CLI Setup

```bash
npm i -g @anthropic-ai/claude-code   # Node 18+ required
cd your-project
claude                                # starts the session, login on first run
```